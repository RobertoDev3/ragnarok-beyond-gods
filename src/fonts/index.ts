import { Baloo_2, Rouge_Script } from 'next/font/google';

export const baloo2 = Baloo_2({
  weight: ['400', '500', '600', '700'],
  subsets: ['latin'],
  display: 'swap',
});

export const rougeScript = Rouge_Script({
  weight: ['400'],
  subsets: ['latin'],
  display: 'swap',
});
